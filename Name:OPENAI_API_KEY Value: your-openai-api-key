4. Commit the workflow file at `.github/workflows/ai_assistant.yml`.

---

## 🧠 Example Workflow Snippet

```yaml
name: AI Assistant Issue Reply
on:
issues:
 types: [opened]

jobs:
reply:
 runs-on: ubuntu-latest
 steps:
   - uses: actions/checkout@v4
   - name: Set up Python
     uses: actions/setup-python@v5
     with:
       python-version: '3.10'
   - name: Install dependencies
     run: pip install -r requirements.txt
   - name: Run AI Assistant
     env:
       OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
     run: python github-ai-assistant/python/main.py
