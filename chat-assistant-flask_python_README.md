# Chat Assistant (Python / Flask)

This is a simple web-based AI chat assistant using **Flask** and the **OpenAI API**.

---

## 🧰 Requirements

- Python 3.9+  
- Flask  
- Requests  
- OpenAI API key (`OPENAI_API_KEY`)

Install dependencies:

```bash
pip install -r requirements.txt
